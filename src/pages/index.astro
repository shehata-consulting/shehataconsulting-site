---
// src/pages/index.astro
// Homepage — hero, services, approach, about summary, and contact CTA.

import Layout from "@/layouts/Layout.astro";
---

<Layout
  title="Shehata Tech Solutions — Enterprise AI Implementation & Automation"
  description="I help businesses navigate the noise of modern AI to build tools that actually work. Hybrid RAG systems, LLM integrations, and process automation."
>

  <!-- ─── Hero ───────────────────────────────────────── -->
  <section class="hero">
    <div class="hero-body">
      <p class="hero-kicker">AI Solutions &amp; Automation Consulting</p>
      <h1 class="hero-headline">
        AI that actually<br />
        <em>works</em> for your<br />
        business.
      </h1>
      <div class="hero-row">
        <p class="hero-sub">
          Enterprise-grade AI implementation and automation — built around
          your operations, not around the hype.
        </p>
        <div class="hero-cta">
          <a href="#services" class="btn-main">
            What I Build
            <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
              <path d="M2 7h10M8 3l4 4-4 4" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
          <a href="#contact" class="btn-line">Start a conversation →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ─── Services ────────────────────────────────────── -->
  <section class="section" id="services">
    <div class="section-inner">
      <p class="section-label reveal">What I Build</p>
      <div class="services-layout">

        <div class="services-left reveal reveal-d1">
          <h2>AI implementations that solve real problems.</h2>
          <p>
            I prefer proven, stable solutions over experimental trends. If a
            simple automation script solves the problem better than a complex
            AI model, I will tell you.
          </p>
        </div>

        <div class="services-list reveal reveal-d2">
          <div class="svc">
            <span class="svc-num">01</span>
            <div>
              <div class="svc-title">Hybrid RAG Systems</div>
              <div class="svc-desc">
                Retrieval-Augmented Generation architectures that give your
                teams instant, accurate access to internal knowledge —
                securely, without sending your data to third parties.
              </div>
            </div>
            <span class="svc-arrow" aria-hidden="true">↗</span>
          </div>

          <div class="svc">
            <span class="svc-num">02</span>
            <div>
              <div class="svc-title">Advanced LLM Implementations</div>
              <div class="svc-desc">
                Custom large language model integrations tailored to your
                industry's specific workflows, data structures, and compliance
                requirements.
              </div>
            </div>
            <span class="svc-arrow" aria-hidden="true">↗</span>
          </div>

          <div class="svc">
            <span class="svc-num">03</span>
            <div>
              <div class="svc-title">Process Automation</div>
              <div class="svc-desc">
                Connecting disconnected systems to eliminate manual data entry
                and reduce operational friction — Python-first, built to last.
              </div>
            </div>
            <span class="svc-arrow" aria-hidden="true">↗</span>
          </div>

          <div class="svc">
            <span class="svc-num">04</span>
            <div>
              <div class="svc-title">AI Feasibility &amp; Strategy</div>
              <div class="svc-desc">
                Not sure if AI is the right tool? I help you cut through the
                noise and identify where automation genuinely moves the needle
                for your business.
              </div>
            </div>
            <span class="svc-arrow" aria-hidden="true">↗</span>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ─── Approach ─────────────────────────────────────── -->
  <section class="section approach-bg" id="approach">
    <div class="section-inner">
      <p class="section-label reveal">My Approach</p>
      <div class="approach-steps reveal reveal-d1">

        <div class="pstep">
          <div class="pstep-n">01</div>
          <h3>Understand First</h3>
          <p>
            I start by learning your operational bottlenecks — not by pitching
            a solution. Technology should serve your goals, not the other way
            around.
          </p>
        </div>

        <div class="pstep">
          <div class="pstep-n">02</div>
          <h3>Build Practically</h3>
          <p>
            Proven and stable over experimental. If a simpler solution fits
            better, I'll say so. Every line of code is written to be maintained
            and scaled.
          </p>
        </div>

        <div class="pstep">
          <div class="pstep-n">03</div>
          <h3>Secure by Default</h3>
          <p>
            Your data is your competitive advantage. Every system I build
            prioritizes privacy and keeps your internal knowledge secure.
          </p>
        </div>

      </div>
    </div>
  </section>

  <!-- ─── Contact CTA ──────────────────────────────────── -->
  <section class="cta-section" id="contact">
    <div class="cta-inner">
      <div class="cta-top reveal">
        <h2 class="cta-heading">
          Ready to build<br />
          <em>something real?</em>
        </h2>
        <div class="cta-right">
          <p>
            Whether you're ready to build or just have questions about
            feasibility, I'm always happy to share what I know. No pitch —
            just an honest conversation about your problem.
          </p>
          <a href="mailto:shehatam.dev@gmail.com" class="btn-main">
            Get in Touch
            <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
              <path d="M2 7h10M8 3l4 4-4 4" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
        </div>
      </div>

      <div class="cta-bottom reveal reveal-d1">
        <a href="shehatam.dev@gmail.com" class="cta-email">
          shehatam.dev@gmail.com
        </a>
        <div class="cta-tags">
          <span class="cta-tag">Hybrid RAG</span>
          <span class="cta-tag">LLM Integration</span>
          <span class="cta-tag">Process Automation</span>
          <span class="cta-tag">Python Development</span>
          <span class="cta-tag">AI Strategy</span>
        </div>
      </div>
    </div>
  </section>

</Layout>

<style>
/* ─── Hero ───────────────────────────────────────────── */
.hero {
  min-height: 100vh;
  display: grid;
  grid-template-rows: 1fr auto;
  padding: 0 5vw;
  position: relative;
  overflow: hidden;
}

.hero::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image:
    linear-gradient(var(--line) 1px, transparent 1px),
    linear-gradient(90deg, var(--line) 1px, transparent 1px);
  background-size: 80px 80px;
  opacity: 0.25;
  mask-image: radial-gradient(ellipse 80% 70% at 50% 40%, black 30%, transparent 80%);
  -webkit-mask-image: radial-gradient(ellipse 80% 70% at 50% 40%, black 30%, transparent 80%);
}

.hero-body {
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  padding-bottom: 5rem;
  padding-top: 12rem;
  max-width: 1200px;
  width: 100%;
}

.hero-kicker {
  display: flex;
  align-items: center;
  gap: 1rem;
  font-size: 0.7rem;
  font-weight: 500;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: var(--blue);
  margin-bottom: 2.5rem;
  opacity: 0;
  animation: up 0.7s 0.1s ease forwards;
}

.hero-kicker::before {
  content: '';
  display: block;
  width: 36px;
  height: 1px;
  background: currentColor;
}

.hero-headline {
  font-family: var(--serif);
  font-size: clamp(3.5rem, 8vw, 8rem);
  font-weight: 300;
  line-height: 0.95;
  letter-spacing: -0.01em;
  color: var(--white);
  opacity: 0;
  animation: up 0.8s 0.2s ease forwards;
}

.hero-headline em {
  font-style: italic;
  color: transparent;
  -webkit-text-stroke: 1px rgba(240, 240, 246, 0.35);
}

.hero-row {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 3rem;
  margin-top: 4rem;
  padding-top: 2.5rem;
  border-top: 1px solid var(--line);
  flex-wrap: wrap;
  opacity: 0;
  animation: up 0.7s 0.4s ease forwards;
}

.hero-sub {
  max-width: 420px;
  font-size: 1rem;
  color: var(--smoke);
  line-height: 1.8;
  font-weight: 300;
}

.hero-cta {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  flex-wrap: wrap;
}

@keyframes up {
  from { opacity: 0; transform: translateY(20px); }
  to   { opacity: 1; transform: translateY(0); }
}

/* ─── Services ───────────────────────────────────────── */
.services-layout {
  display: grid;
  grid-template-columns: 1fr 2fr;
  gap: 8vw;
  align-items: start;
}

.services-left h2 {
  font-family: var(--serif);
  font-size: clamp(2.2rem, 4vw, 3.8rem);
  font-weight: 300;
  line-height: 1.15;
  color: var(--white);
  margin-bottom: 1.5rem;
}

.services-left p {
  color: var(--smoke);
  font-size: 0.95rem;
  line-height: 1.8;
}

.services-list {
  display: flex;
  flex-direction: column;
}

.svc {
  display: grid;
  grid-template-columns: auto 1fr auto;
  align-items: start;
  gap: 0 2rem;
  padding: 2.25rem 0;
  border-bottom: 1px solid var(--line);
  cursor: default;
  position: relative;
}

.svc:first-child {
  border-top: 1px solid var(--line);
}

.svc::before {
  content: '';
  position: absolute;
  left: -2rem;
  top: 0;
  bottom: 0;
  width: 2px;
  background: var(--blue);
  transform: scaleY(0);
  transform-origin: top;
  transition: transform 0.35s ease;
}

.svc:hover::before {
  transform: scaleY(1);
}

.svc-num {
  font-family: var(--serif);
  font-size: 0.85rem;
  font-weight: 300;
  color: var(--dim);
  padding-top: 0.25rem;
  min-width: 2rem;
}

.svc-title {
  font-family: var(--serif);
  font-size: 1.45rem;
  font-weight: 300;
  color: var(--white);
  letter-spacing: -0.01em;
  line-height: 1.3;
  margin-bottom: 0.5rem;
}

.svc-desc {
  color: var(--smoke);
  font-size: 0.88rem;
  line-height: 1.7;
  max-width: 480px;
}

.svc-arrow {
  color: var(--dim);
  font-size: 1.1rem;
  padding-top: 0.3rem;
  transition: color 0.2s, transform 0.2s;
}

.svc:hover .svc-arrow {
  color: var(--blue);
  transform: translate(3px, -3px);
}

/* ─── Approach ───────────────────────────────────────── */
.approach-bg {
  background: var(--ink2);
}

.approach-steps {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0;
  border: 1px solid var(--line);
  border-radius: 4px;
  overflow: hidden;
}

.pstep {
  padding: 2.5rem 2rem;
  border-right: 1px solid var(--line);
  transition: background 0.25s;
}

.pstep:last-child {
  border-right: none;
}

.pstep:hover {
  background: rgba(79, 110, 247, 0.05);
}

.pstep-n {
  font-family: var(--serif);
  font-size: 3rem;
  font-weight: 300;
  color: var(--line);
  line-height: 1;
  margin-bottom: 1.25rem;
}

.pstep h3 {
  font-family: var(--serif);
  font-size: 1.1rem;
  font-weight: 400;
  color: var(--white);
  margin-bottom: 0.75rem;
}

.pstep p {
  font-size: 0.82rem;
  color: var(--smoke);
  line-height: 1.7;
}

/* ─── CTA ────────────────────────────────────────────── */
.cta-section {
  padding: 9rem 5vw;
  position: relative;
  overflow: hidden;
}

.cta-section::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(ellipse 60% 80% at 50% 50%, rgba(79, 110, 247, 0.07) 0%, transparent 70%);
  pointer-events: none;
}

.cta-inner {
  max-width: 1200px;
  margin: 0 auto;
  position: relative;
}

.cta-top {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 3rem;
  flex-wrap: wrap;
  padding-bottom: 3.5rem;
  border-bottom: 1px solid var(--line);
  margin-bottom: 3.5rem;
}

.cta-heading {
  font-family: var(--serif);
  font-size: clamp(2.5rem, 5vw, 5.5rem);
  font-weight: 300;
  line-height: 0.95;
  color: var(--white);
  letter-spacing: -0.02em;
}

.cta-heading em {
  font-style: italic;
  color: var(--blue);
}

.cta-right {
  max-width: 340px;
}

.cta-right p {
  color: var(--smoke);
  font-size: 0.9rem;
  line-height: 1.8;
  margin-bottom: 1.75rem;
}

.cta-bottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 1.5rem;
}

.cta-email {
  font-family: var(--serif);
  font-size: clamp(1.1rem, 2vw, 1.6rem);
  font-weight: 300;
  color: var(--smoke);
  letter-spacing: 0.04em;
  display: flex;
  align-items: center;
  gap: 1rem;
  transition: color 0.2s;
}

.cta-email:hover {
  color: var(--white);
}

.cta-email::before {
  content: '↗';
  font-family: var(--sans);
  font-size: 0.9rem;
  color: var(--blue);
}

.cta-tags {
  display: flex;
  gap: 0.75rem;
  flex-wrap: wrap;
}

.cta-tag {
  padding: 0.35rem 0.9rem;
  border: 1px solid var(--line);
  border-radius: 2px;
  font-size: 0.68rem;
  font-weight: 500;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--dim);
  transition: border-color 0.2s, color 0.2s;
}

.cta-tag:hover {
  border-color: var(--blue);
  color: var(--blue);
}

/* ─── Responsive ─────────────────────────────────────── */

/* Tablet + mobile (≤ 900px) */
@media (max-width: 900px) {
  /* Hero */
  .hero-body {
    padding-top: 8rem;
    padding-bottom: 3.5rem;
  }

  .hero-row {
    flex-direction: column;
    align-items: flex-start;
    gap: 2rem;
    margin-top: 2.5rem;
    padding-top: 2rem;
  }

  .hero-sub {
    max-width: 100%;
  }

  .hero-cta {
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
    width: 100%;
  }

  /* Services: stack columns */
  .services-layout {
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }

  /* Service rows: tighten gap between number and text */
  .svc {
    gap: 0 1.25rem;
    padding: 1.75rem 0;
  }

  /* Approach: stack to single column */
  .approach-steps {
    grid-template-columns: 1fr;
    border-radius: 4px;
  }

  .pstep {
    border-right: none;
    border-bottom: 1px solid var(--line);
  }

  .pstep:last-child {
    border-bottom: none;
  }

  /* CTA */
  .cta-section {
    padding: 5rem 5vw;
  }

  .cta-top {
    flex-direction: column;
    align-items: flex-start;
    padding-bottom: 2.5rem;
    margin-bottom: 2.5rem;
  }

  .cta-right {
    max-width: 100%;
  }

  .cta-bottom {
    flex-direction: column;
    align-items: flex-start;
    gap: 1.5rem;
  }

  .cta-tags {
    gap: 0.5rem;
  }
}

/* Mobile only (≤ 640px) */
@media (max-width: 640px) {
  /* Hero headline: prevent overflow */
  .hero-headline {
    font-size: clamp(2.8rem, 11vw, 5rem);
    line-height: 1;
  }

  .hero-kicker {
    font-size: 0.62rem;
    margin-bottom: 1.75rem;
  }

  /* Hero grid background: subtler on small screens */
  .hero::before {
    background-size: 50px 50px;
    opacity: 0.15;
  }

  /* Service numbers: hide on very small screens to save space */
  .svc-num {
    display: none;
  }

  .svc {
    grid-template-columns: 1fr auto;
  }

  .svc-title {
    font-size: 1.2rem;
  }

  /* Approach step numbers: smaller */
  .pstep-n {
    font-size: 2rem;
    margin-bottom: 0.75rem;
  }

  /* CTA heading */
  .cta-heading {
    font-size: clamp(2rem, 10vw, 3rem);
  }
}
</style>